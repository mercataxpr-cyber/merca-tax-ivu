/* GENERATED by scripts/build.mjs — edit src/domain.js, src/tax-remediation.js, src/tax-calendar-contract.js, src/app.js, src/tax-ui-bridge.js or src/mobile-r1-ui.js, not script.js. */
(function loadMercaTaxSources() {
  'use strict';

  const current = document.currentScript;
  const baseUrl = current && current.src
    ? new URL('.', current.src)
    : new URL('.', window.location.href);

  function load(path, onload) {
    const element = document.createElement('script');
    element.src = new URL(path, baseUrl).href;
    element.async = false;
    element.onload = onload;
    element.onerror = () => {
      throw new Error(`Failed to load ${path}`);
    };
    document.head.appendChild(element);
  }

  function start() {
    load('src/domain.js', () =>
      load('src/tax-remediation.js', () =>
        load('src/tax-calendar-contract.js', () =>
          load('src/app.js', () =>
            load('src/tax-ui-bridge.js', () =>
              load('src/mobile-r1-ui.js', () => {}))))));
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', start, { once: true });
  } else {
    start();
  }
}());
