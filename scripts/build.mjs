import { writeFileSync } from 'node:fs';

const loader = `/* GENERATED by scripts/build.mjs — edit src/domain.js, src/app.js or src/mobile-r1-ui.js, not script.js. */
(function loadMercaTaxSources() {
  'use strict';

  const current = document.currentScript;
  const baseUrl = current && current.src
    ? new URL('.', current.src)
    : new URL('.', window.location.href);

  function load(path, onload) {
    const element = document.createElement('script');
    element.src = new URL(path, baseUrl).href;
    element.async = false;
    element.onload = onload;
    element.onerror = () => {
      throw new Error(`Failed to load ${path}`);
    };
    document.head.appendChild(element);
  }

  function start() {
    load('src/domain.js', () => load('src/app.js', () => load('src/mobile-r1-ui.js', () => {})));
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', start, { once: true });
  } else {
    start();
  }
}());
`;

writeFileSync('script.js', loader);
console.log('Generated script.js loader for src/domain.js -> src/app.js -> src/mobile-r1-ui.js');
